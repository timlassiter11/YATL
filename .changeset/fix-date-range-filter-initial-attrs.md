---
"@timlassiter11/yatl-ui": patch
---

Fixed `yatl-date-range-filter` not applying `start-date`/`end-date` set via attribute until the user manually changed the range - the dates displayed as if filtering, but no filter was ever applied
